# Normalize to LF in the repository; Git still checks out CRLF on Windows.
* text=auto

# The Maven wrapper script must stay LF or it will not run on Linux/macOS.
mvnw text eol=lf

*.cmd text eol=crlf
*.bat text eol=crlf

*.jar binary
